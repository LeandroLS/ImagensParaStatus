<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Imagem Para Status</title>
    style(type="text/css").
        a:hover { 
            background-color: yellow;
        }
    include layouts/default-css-and-js.pug
    
</head>

<body>
    header
        include layouts/header.pug
    if(images.length >= 1)
        .container-fluid
            .row
                .offset-md-1.col-md-7
                    each image in images
                        div.card
                            div.card-body
                                p.blockquote
                                    img(src=`images/original-images/${image.fileName}`, alt=`${image.phrase}`, class="img-fluid")
                                <p class="card-text"> #{image.phrase} </p>
                                <p class="card-text"> Categoria: #{image.category} </p>
                .col-md-4
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <h3 class="panel-title"> Categorias </h3>
                        </div>
                        if(categories)
                            <ul class="list-group">
                                for categorie in categories
                                    <a href="#" class="list-group-item"> #{categorie.category} </a>
                            </ul>
                    </div>
    else 
        p.bg-warning Nenhuma imagem encontrada.
</body>

</html>